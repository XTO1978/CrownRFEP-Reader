<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CrownRFEP_Reader.ViewModels"
             xmlns:models="clr-namespace:CrownRFEP_Reader.Models"
             xmlns:controls="clr-namespace:CrownRFEP_Reader.Views.Controls"
             x:Class="CrownRFEP_Reader.Views.SessionsPage"
             x:DataType="vm:SessionsViewModel"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto,Auto,Auto,*,Auto" Padding="15">

        <controls:TopTabsBar Grid.Row="0" />
        
        <!-- Acceso a Videolecciones (como una sesión más) -->
        <Frame Grid.Row="1" Margin="0,8" Padding="15" CornerRadius="12" HasShadow="True">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ViewVideoLessonsCommand}" />
            </Frame.GestureRecognizers>

            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                <Label Text="🎥" FontSize="{StaticResource FontSizeTitle}" VerticalOptions="Center" />
                <VerticalStackLayout Grid.Column="1" Spacing="2">
                    <Label Text="Videolecciones" FontSize="{StaticResource FontSizeSubtitle}" TextColor="White" FontAttributes="Bold" />
                    <Label Text="Todas las videolecciones guardadas" FontSize="{StaticResource FontSizeSmall}" TextColor="#FFAAAAAA" />
                </VerticalStackLayout>
            </Grid>
        </Frame>

        <!-- Barra de búsqueda -->
        <SearchBar Grid.Row="2"
                   Placeholder="Buscar sesiones..."
                   Text="{Binding SearchText}"
                   BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}"
                   CancelButtonColor="{StaticResource Primary}"/>

        <RefreshView Grid.Row="3" 
                     Command="{Binding RefreshCommand}" 
                     IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding FilteredSessions}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Session">
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="Eliminar"
                                               BackgroundColor="Red"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SessionsViewModel}}, Path=DeleteSessionCommand}"
                                               CommandParameter="{Binding .}"/>
                                </SwipeItems>
                            </SwipeView.RightItems>
                            
                            <Frame Margin="0,8" Padding="15" CornerRadius="12" HasShadow="True">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SessionsViewModel}}, Path=ViewSessionCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Frame.GestureRecognizers>
                                
                                <Grid ColumnDefinitions="*,Auto" Padding="0">
                                    <Label Grid.Column="0"
                                           FontSize="{StaticResource FontSizeSubtitle}"
                                           TextColor="{AppThemeBinding Light={StaticResource PrimaryTextColor}, Dark={StaticResource PrimaryTextColor}}"
                                           LineBreakMode="TailTruncation"
                                           VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding FechaDateTime, StringFormat='{0:dd/MM/yyyy}'}" />
                                                <Span Text="  ·  " />
                                                <Span Text="{Binding Lugar}" />
                                                <Span Text="  ·  " />
                                                <Span Text="{Binding DisplayName}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    
                                    <!-- Botón Grabar (solo iOS) -->
                                    <Button Grid.Column="1"
                                            Text="🎥"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RecordVideoCommand}"
                                            CommandParameter="{Binding .}"
                                            IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CanRecordVideo}"
                                            BackgroundColor="{StaticResource Primary}"
                                            TextColor="White"
                                            CornerRadius="20"
                                            WidthRequest="40"
                                            HeightRequest="40"
                                            Padding="0"
                                            VerticalOptions="Center" />
                                </Grid>
                            </Frame>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="40">
                        <Label Text="🔍" FontSize="{StaticResource FontSizeDisplay}" HorizontalOptions="Center"/>
                        <Label Text="No se encontraron sesiones" 
                               FontSize="{StaticResource FontSizeTitle}" 
                               HorizontalOptions="Center" 
                               TextColor="Gray"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>

        <!--  Footer de la aplicación  -->
        <controls:AppFooter Grid.Row="4" />
    </Grid>
</ContentPage>
