<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CrownRFEP_Reader.Views.ImportPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behaviors="clr-namespace:CrownRFEP_Reader.Behaviors"
    xmlns:controls="clr-namespace:CrownRFEP_Reader.Views.Controls"
    xmlns:models="clr-namespace:CrownRFEP_Reader.Models"
    xmlns:vm="clr-namespace:CrownRFEP_Reader.ViewModels"
    x:DataType="vm:ImportViewModel"
    BackgroundColor="#FF121212">

    <Grid RowDefinitions="Auto,*" RowSpacing="0">
        <controls:TopTabsBar
            Grid.Row="0"
            HorizontalOptions="Center"
            VerticalOptions="Start" />

        <Grid
            Grid.Row="1"
            Padding="16"
            ColumnDefinitions="340,*,1.3*"
            ColumnSpacing="12">

            <!--  COLUMNA 1: Árbol de carpetas  -->
            <Border
                Grid.Column="0"
                BackgroundColor="#FF1E1E1E"
                Stroke="#FF3B3B3B"
                StrokeShape="RoundRectangle 10"
                StrokeThickness="1">
                <Grid RowDefinitions="Auto,*,Auto" RowSpacing="0">
                    <!--  Cabecera  -->
                    <Border
                        Grid.Row="0"
                        Padding="10,8"
                        BackgroundColor="#FF2D2D2D"
                        StrokeThickness="0">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                            <Button
                                Grid.Column="0"
                                Padding="6"
                                BackgroundColor="Transparent"
                                BorderWidth="0"
                                Command="{Binding RefreshCommand}"
                                HeightRequest="30"
                                WidthRequest="30">
                                <Button.ImageSource>
                                    <FontImageSource
                                        Glyph="↻"
                                        Size="14"
                                        Color="#FF878787" />
                                </Button.ImageSource>
                            </Button>

                            <Label
                                Grid.Column="1"
                                FontAttributes="Bold"
                                FontSize="13"
                                Text="📁 Carpetas"
                                TextColor="White"
                                VerticalOptions="Center" />

                            <Button
                                Grid.Column="2"
                                Padding="8,4"
                                BackgroundColor="#FF3B3B3B"
                                Command="{Binding AddVideosWithPickerCommand}"
                                CornerRadius="4"
                                FontSize="10"
                                Text="Buscar..."
                                TextColor="White" />
                        </Grid>
                    </Border>

                    <!--  Árbol de carpetas  -->
                    <CollectionView
                        Grid.Row="1"
                        BackgroundColor="Transparent"
                        ItemsSource="{Binding FlatItems}"
                        SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                        SelectionMode="Single">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:FileSystemItem">
                                <Border
                                    Padding="0"
                                    BackgroundColor="{Binding IsSelected, Converter={StaticResource BoolToColorConverter}}"
                                    HeightRequest="28"
                                    StrokeThickness="0">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ImportViewModel}}, Path=SelectItemCommand}" CommandParameter="{Binding .}" />
                                        <TapGestureRecognizer
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ImportViewModel}}, Path=ToggleExpandCommand}"
                                            CommandParameter="{Binding .}"
                                            NumberOfTapsRequired="2" />
                                    </Border.GestureRecognizers>

                                    <Grid
                                        Padding="6,0"
                                        ColumnDefinitions="Auto,Auto,Auto,*"
                                        ColumnSpacing="3">
                                        <BoxView
                                            Grid.Column="0"
                                            BackgroundColor="Transparent"
                                            WidthRequest="{Binding Indentation}" />

                                        <Border
                                            Grid.Column="1"
                                            BackgroundColor="Transparent"
                                            HeightRequest="18"
                                            IsVisible="{Binding CanExpand}"
                                            WidthRequest="18" StrokeThickness="0">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ImportViewModel}}, Path=ToggleExpandCommand}" CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                            <Grid>
                                                <ActivityIndicator
                                                    HeightRequest="12"
                                                    IsRunning="{Binding IsLoading}"
                                                    IsVisible="{Binding IsLoading}"
                                                    WidthRequest="12"
                                                    Color="#FF878787" />
                                                <controls:SymbolIcon
                                                    HeightRequest="14"
                                                    HorizontalOptions="Center"
                                                    IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"
                                                    SymbolName="{Binding ExpanderIcon}"
                                                    TintColor="White"
                                                    VerticalOptions="Center"
                                                    WidthRequest="14" />
                                            </Grid>
                                        </Border>

                                        <BoxView
                                            Grid.Column="1"
                                            BackgroundColor="Transparent"
                                            IsVisible="{Binding CanExpand, Converter={StaticResource InvertedBoolConverter}}"
                                            WidthRequest="18" />

                                        <controls:SymbolIcon
                                            Grid.Column="2"
                                            Margin="2,0"
                                            HeightRequest="24"
                                            SymbolName="folder.fill"
                                            TintColor="{Binding IconColor}"
                                            WidthRequest="24" />

                                        <Label
                                            Grid.Column="3"
                                            FontSize="14"
                                            LineBreakMode="TailTruncation"
                                            Text="{Binding Name}"
                                            TextColor="{Binding IsCrownFile, Converter={StaticResource CrownFileColorConverter}}"
                                            VerticalOptions="Center" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <!--  Ruta actual  -->
                    <Border
                        Grid.Row="2"
                        Padding="10,6"
                        BackgroundColor="#FF2D2D2D"
                        StrokeThickness="0">
                        <Label
                            FontSize="10"
                            LineBreakMode="TailTruncation"
                            Text="{Binding CurrentPath}"
                            TextColor="#FF666666" />
                    </Border>
                </Grid>
            </Border>

            <!--  COLUMNA 2: Archivos de video de la carpeta seleccionada  -->
            <Border
                Grid.Column="1"
                BackgroundColor="#FF1E1E1E"
                Stroke="#FF3B3B3B"
                StrokeShape="RoundRectangle 10"
                StrokeThickness="1">
                <Grid RowDefinitions="Auto,*,Auto" RowSpacing="0">
                    <!--  Cabecera  -->
                    <Border
                        Grid.Row="0"
                        Padding="10,8"
                        BackgroundColor="#FF2D2D2D"
                        StrokeThickness="0">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                            <Label
                                Grid.Column="0"
                                FontSize="14"
                                LineBreakMode="TailTruncation"
                                Text="{Binding SelectedFolderInfo}"
                                TextColor="White"
                                VerticalOptions="Center" />
                            <Button
                                Grid.Column="1"
                                Padding="8,4"
                                BackgroundColor="#FF464646"
                                Command="{Binding AddAllFolderVideosCommand}"
                                CornerRadius="4"
                                FontSize="10"
                                Text="+ Todos"
                                TextColor="White" />
                        </Grid>
                    </Border>

                    <!--  Lista de archivos de video  -->
                    <Grid Grid.Row="1">
                        <!--  Placeholder si no hay videos  -->
                        <VerticalStackLayout
                            HorizontalOptions="Center"
                            IsVisible="{Binding FolderFiles.Count, Converter={StaticResource IsZeroConverter}}"
                            Spacing="8"
                            VerticalOptions="Center">
                            <Label
                                FontSize="11"
                                HorizontalOptions="Center"
                                Text="Selecciona una carpeta"
                                TextColor="#FF666666" />
                        </VerticalStackLayout>

                        <!--  Lista de videos  -->
                        <CollectionView
                            Margin="4,0,4,0"
                            BackgroundColor="Transparent"
                            ItemsSource="{Binding FolderFiles}"
                            SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:FileSystemItem">
                                    <Border
                                        Margin="0"
                                        Padding="6,2"
                                        BackgroundColor="#FF2A2A2A"
                                        StrokeShape="RoundRectangle 4"
                                        StrokeThickness="0">
                                        <Border.GestureRecognizers>
                                            <DragGestureRecognizer
                                                CanDrag="True"
                                                DragStarting="OnVideoDragStarting" />
                                        </Border.GestureRecognizers>
                                        <Grid ColumnDefinitions="Auto,Auto,*,Auto" ColumnSpacing="4">
                                            <CheckBox
                                                Grid.Column="0"
                                                IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                Color="#FF6DDDFF"
                                                VerticalOptions="Center" />
                                            <controls:SymbolIcon
                                                Grid.Column="1"
                                                HeightRequest="24"
                                                SymbolName="{Binding Icon}"
                                                TintColor="#FFD9D9D9"
                                                WidthRequest="24" />

                                            <Label
                                                Grid.Column="2"
                                                FontSize="14"
                                                LineBreakMode="TailTruncation"
                                                Text="{Binding Name}"
                                                TextColor="White"
                                                VerticalOptions="Center" />

                                            <Button
                                                Grid.Column="3"
                                                Padding="8,6"
                                                BackgroundColor="#FF464646"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ImportViewModel}}, Path=AddVideoFromFolderListCommand}"
                                                CommandParameter="{Binding .}"
                                                CornerRadius="4"
                                                FontSize="14"
                                                HeightRequest="24"
                                                Text="+"
                                                TextColor="White"
                                                WidthRequest="38" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Grid>

                    <!--  Info de archivos  -->
                    <Border
                        Grid.Row="2"
                        Padding="10,6"
                        BackgroundColor="#FF2D2D2D"
                        StrokeThickness="0">
                        <Label
                            FontSize="10"
                            Text="{Binding FolderFilesInfo}"
                            TextColor="#FF878787" />
                    </Border>
                </Grid>
            </Border>

            <!--  COLUMNA 3: Zona de Drop y Sesión Customizada  -->
            <Grid Grid.Column="2" RowDefinitions="*,Auto">

                <!--  Panel de videos pendientes con zona de drop  -->
                <Border
                    Grid.Row="0"
                    BackgroundColor="{Binding IsDragOver, Converter={StaticResource BoolToColorConverter}, ConverterParameter=#FF2A4A6A|#FF1E1E1E}"
                    Stroke="{Binding IsDragOver, Converter={StaticResource BoolToColorConverter}, ConverterParameter=#FF4A9ADA|#FF3B3B3B}"
                    StrokeDashArray="5,3"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="2">

                    <!--  Drop gesture recognizer  -->
                    <Border.GestureRecognizers>
                        <DropGestureRecognizer AllowDrop="True" />
                    </Border.GestureRecognizers>
                    <Border.Behaviors>
                        <behaviors:DropZoneBehavior DropCommand="{Binding FilesDroppedCommand}" IsDragOver="{Binding IsDragOver, Mode=TwoWay}" />
                    </Border.Behaviors>

                    <Grid RowDefinitions="Auto,*,Auto" RowSpacing="0">
                        <!--  Cabecera  -->
                        <Border
                            Grid.Row="0"
                            Padding="12,10"
                            BackgroundColor="#FF2D2D2D"
                            StrokeThickness="0">
                            <Grid ColumnDefinitions="*,Auto">
                                <Label
                                    Grid.Column="0"
                                    FontAttributes="Bold"
                                    FontSize="14"
                                    Text="Videos para Sesión"
                                    TextColor="White" />
                                <Button
                                    Grid.Column="1"
                                    Padding="8,4"
                                    BackgroundColor="#FF5A2A2A"
                                    Command="{Binding ClearAllVideosCommand}"
                                    CornerRadius="4"
                                    FontSize="11"
                                    Text="Limpiar"
                                    TextColor="White" />
                            </Grid>
                        </Border>

                        <!--  Lista de videos o placeholder  -->
                        <Grid Grid.Row="1">
                            <!--  Placeholder cuando no hay videos  -->
                            <VerticalStackLayout
                                HorizontalOptions="Center"
                                IsVisible="{Binding PendingVideos.Count, Converter={StaticResource IsZeroConverter}}"
                                Spacing="12"
                                VerticalOptions="Center">
                                <Label
                                    FontSize="16"
                                    HorizontalOptions="Center"
                                    Text="Arrastra videos aquí"
                                    TextColor="#FF878787" />
                                <Label
                                    FontSize="12"
                                    HorizontalOptions="Center"
                                    Text="o usa los botones del explorador"
                                    TextColor="#FF666666" />
                            </VerticalStackLayout>

                            <!--  Lista de videos pendientes  -->
                            <CollectionView
                                Margin="8"
                                BackgroundColor="Transparent"
                                ItemsSource="{Binding PendingVideos}"
                                SelectionMode="None">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="vm:PendingVideoItem">
                                        <Border
                                            Margin="0,4"
                                            Padding="12"
                                            BackgroundColor="#FF2A2A2A"
                                            Stroke="#FF3B3B3B"
                                            StrokeShape="RoundRectangle 8"
                                            StrokeThickness="1">
                                            <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="10">
                                                <!--  Icono  -->
                                                <controls:SymbolIcon
                                                    Grid.Column="0"
                                                    HeightRequest="24"
                                                    SymbolName="{Binding Icon}"
                                                    TintColor="{Binding IconColor}"
                                                    WidthRequest="24" />

                                                <!--  Info del video  -->
                                                <VerticalStackLayout Grid.Column="1" Spacing="2">
                                                    <Label
                                                        FontSize="13"
                                                        LineBreakMode="TailTruncation"
                                                        Text="{Binding FileName}"
                                                        TextColor="White" />
                                                    <Label
                                                        FontSize="11"
                                                        Text="{Binding FileSizeFormatted}"
                                                        TextColor="#FF666666" />
                                                </VerticalStackLayout>

                                                <!--  Extensión badge  -->
                                                <Border
                                                    Grid.Column="2"
                                                    Padding="6,2"
                                                    BackgroundColor="#FF3B3B3B"
                                                    StrokeShape="RoundRectangle 4"
                                                    StrokeThickness="0">
                                                    <Label
                                                        FontSize="10"
                                                        Text="{Binding Extension}"
                                                        TextColor="#FF878787"
                                                        TextTransform="Uppercase" />
                                                </Border>

                                                <!--  Botón eliminar  -->
                                                <Button
                                                    Grid.Column="3"
                                                    Padding="4"
                                                    BackgroundColor="Transparent"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ImportViewModel}}, Path=RemoveVideoCommand}"
                                                    CommandParameter="{Binding .}"
                                                    FontSize="14"
                                                    HeightRequest="28"
                                                    Text="✕"
                                                    TextColor="#FF878787"
                                                    WidthRequest="28" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </Grid>

                        <!--  Info de videos  -->
                        <Border
                            Grid.Row="2"
                            Padding="12,8"
                            BackgroundColor="#FF2D2D2D"
                            StrokeThickness="0">
                            <Label
                                FontSize="11"
                                Text="{Binding PendingVideosInfo}"
                                TextColor="#FF878787" />
                        </Border>
                    </Grid>
                </Border>

                <!--  Panel para crear sesión customizada  -->
                <Border
                    Grid.Row="1"
                    Margin="0,16,0,0"
                    Padding="16"
                    BackgroundColor="#FF1E1E1E"
                    Stroke="#FF3B3B3B"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="1">
                    <VerticalStackLayout Spacing="12">
                        <Label
                            FontAttributes="Bold"
                            FontSize="14"
                            Text="Crear Sesión Customizada"
                            TextColor="White" />

                        <!--  Nombre de la sesión  -->
                        <VerticalStackLayout Spacing="4">
                            <Label
                                FontSize="11"
                                Text="Nombre de la sesión"
                                TextColor="#FF878787" />
                            <Entry
                                BackgroundColor="#FF2A2A2A"
                                FontSize="13"
                                Placeholder="Ej: Entrenamiento técnico"
                                PlaceholderColor="#FF666666"
                                Text="{Binding CustomSessionName}"
                                TextColor="White" />
                        </VerticalStackLayout>

                        <!--  Ubicación  -->
                        <VerticalStackLayout Spacing="4">
                            <Label
                                FontSize="11"
                                Text="Ubicación (opcional)"
                                TextColor="#FF878787" />
                            <Entry
                                BackgroundColor="#FF2A2A2A"
                                FontSize="13"
                                Placeholder="Ej: Piscina Municipal"
                                PlaceholderColor="#FF666666"
                                Text="{Binding CustomSessionLocation}"
                                TextColor="White" />
                        </VerticalStackLayout>

                        <!--  Fecha  -->
                        <VerticalStackLayout Spacing="4">
                            <Label
                                FontSize="11"
                                Text="Fecha"
                                TextColor="#FF878787" />
                            <DatePicker
                                BackgroundColor="#FF2A2A2A"
                                Date="{Binding CustomSessionDate}"
                                FontSize="13"
                                TextColor="White" />
                        </VerticalStackLayout>

                        <!--  Botón crear sesión  -->
                        <Button
                            Margin="0,8,0,0"
                            Padding="16,12"
                            BackgroundColor="#FF2E7D32"
                            Command="{Binding CreateCustomSessionCommand}"
                            CornerRadius="8"
                            FontAttributes="Bold"
                            FontSize="14"
                            IsEnabled="{Binding CanCreateCustomSession}"
                            Text="Crear Sesión"
                            TextColor="White" />

                        <!--  Progreso de importación  -->
                        <VerticalStackLayout IsVisible="{Binding IsImporting}" Spacing="8">
                            <ProgressBar
                                HeightRequest="6"
                                Progress="{Binding ImportProgressValue, Converter={StaticResource ProgressConverter}}"
                                ProgressColor="#FF4CAF50" />
                            <Label
                                FontSize="11"
                                HorizontalOptions="Center"
                                Text="{Binding ImportProgressText}"
                                TextColor="#FF878787" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>
            </Grid>

            <!--  Indicador de carga global  -->
            <ActivityIndicator
                Grid.ColumnSpan="3"
                HorizontalOptions="Center"
                IsRunning="{Binding IsBusy}"
                IsVisible="{Binding IsBusy}"
                VerticalOptions="Center"
                Color="{StaticResource Primary}" />
        </Grid>
    </Grid>
</ContentPage>