<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="CrownRFEP_Reader.Views.Controls.TopTabsBar"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="#FF303030">

    <Grid Padding="10,0" ColumnDefinitions="Auto,Auto,Auto,Auto,Auto,Auto" ColumnSpacing="0" HeightRequest="50">
        <Button Grid.Column="0"
                Text="Dashboard"
                Clicked="OnDashboardClicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontFamily="{StaticResource AppFontBody}"
                FontSize="16"
                Padding="16,5"
                CornerRadius="0" /> 
                
                <Button Grid.Column="1"
                Text="Sesiones"
                Clicked="OnSessionsClicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontFamily="{StaticResource AppFontBody}"
                FontSize="16"
                Padding="16,5"
                CornerRadius="0" />

                <Button Grid.Column="2"
                Text="Atletas"
                Clicked="OnAthletesClicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontFamily="{StaticResource AppFontBody}"
                FontSize="16"
                Padding="16,5"
                CornerRadius="0" />
                
                <Button Grid.Column="3"
                Text="Estadísticas"
                Clicked="OnStatsClicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontFamily="{StaticResource AppFontBody}"
                FontSize="16"
                Padding="16,5"
                CornerRadius="0" />
                
                <Button Grid.Column="4"
                Text="Importar"
                Clicked="OnImportClicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontFamily="{StaticResource AppFontBody}"
                FontSize="16"
                Padding="16,5"
                CornerRadius="0" />
                
                <Button Grid.Column="5"
                Text="Yo"
                Clicked="OnProfileClicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontFamily="{StaticResource AppFontBody}"
                FontSize="16"
                Padding="16,5"
                CornerRadius="0" />
    </Grid>
</ContentView>
