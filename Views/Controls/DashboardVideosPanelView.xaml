<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="CrownRFEP_Reader.Views.Controls.DashboardVideosPanelView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:appcontrols="clr-namespace:CrownRFEP_Reader.Controls"
    xmlns:behaviors="clr-namespace:CrownRFEP_Reader.Behaviors"
    xmlns:converters="clr-namespace:CrownRFEP_Reader.Converters"
    xmlns:controls="clr-namespace:CrownRFEP_Reader.Views.Controls"
    xmlns:models="clr-namespace:CrownRFEP_Reader.Models"
    xmlns:vm="clr-namespace:CrownRFEP_Reader.ViewModels"
    x:DataType="vm:DashboardViewModel">

    <ContentView.Resources>
        <ResourceDictionary>
            <converters:NotNullOrEmptyBoolConverter x:Key="NotNullOrEmptyBoolConverter" />
        </ResourceDictionary>
    </ContentView.Resources>

    <!--  Panel derecho: vídeos de la sesión seleccionada  -->
    <Grid Padding="20" RowDefinitions="Auto,Auto,*">

        <!--  Header con título y botón Grabar  -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
            <Label
                Grid.Column="0"
                FontSize="{StaticResource FontSizeTitle}"
                LineBreakMode="TailTruncation"
                Text="{Binding SelectedSessionTitle}"
                TextColor="White"
                VerticalOptions="Center" />

            <!--  Botón Grabar (solo iOS — en macOS/Windows no hay grabación con cámara)  -->
            <Border
                Grid.Column="1"
                Padding="12,8"
                BackgroundColor="#FFFF3B30"
                Stroke="Transparent"
                StrokeShape="RoundRectangle 4"
                IsVisible="{OnPlatform Default=False, iOS={Binding CanShowRecordButton}}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding RecordForSelectedSessionCommand}" />
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="6">
                    <controls:SymbolIcon
                        HeightRequest="{StaticResource IconSizeMedium}"
                        SymbolName="video.fill"
                        TintColor="White"
                        WidthRequest="{StaticResource IconSizeMedium}"
                        VerticalOptions="Center" />
                    <Label
                        Text="Grabar"
                        TextColor="White"
                        FontAttributes="None"
                        FontSize="{StaticResource FontSizeBody}"
                        VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>
        </Grid>

        <HorizontalStackLayout
            Grid.Row="1"
            Spacing="10"
            VerticalOptions="Center">
            <Label
                FontSize="{StaticResource FontSizeBody}"
                Text="{Binding Videos.VideoCountDisplayText}"
                TextColor="#FFCCCCCC" />
            <ActivityIndicator
                HeightRequest="16"
                IsRunning="{Binding Videos.IsLoadingSelectedSessionVideos}"
                IsVisible="{Binding Videos.IsLoadingSelectedSessionVideos}"
                WidthRequest="16"
                Color="White" />
        </HorizontalStackLayout>

        <CollectionView
            x:Name="VideoGallery"
            Grid.Row="2"
            ItemsSource="{Binding Videos.SelectedSessionVideos}"
            IsVisible="{Binding Videos.ShowVideoGallery}"
            RemainingItemsThreshold="{OnPlatform MacCatalyst=50, WinUI=10, iOS=10, Default=10}"
            RemainingItemsThresholdReachedCommand="{Binding LoadMoreVideosCommand}"
            SelectionMode="None"
            Margin="{StaticResource MarginTopSmall}"
            ItemSizingStrategy="MeasureFirstItem"
            ItemsUpdatingScrollMode="KeepScrollOffset"
            Scrolled="OnVideoGalleryScrolledInternal">
            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    HorizontalItemSpacing="{StaticResource SpacingLarge}"
                    Orientation="Vertical"
                    Span="{Binding Videos.VideoGalleryColumnSpan}"
                    VerticalItemSpacing="50" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:VideoClip">
                    <Grid RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="4">
                        <!--  Fila 0: Thumbnail con Border  -->
                        <Border
                            Grid.Row="0"
                            BackgroundColor="Transparent"
                            Stroke="Transparent"
                            StrokeShape="RoundRectangle 10"
                            StrokeThickness="0">
                            <Border.Behaviors>
                                <!-- HoverBackgroundBehavior solo activo en Windows (código protegido con #if WINDOWS) -->
                                <behaviors:HoverBackgroundBehavior />
                            </Border.Behaviors>
                            <Border.GestureRecognizers>
                                <!-- iOS: clic simple abre reproductor. MacCatalyst/Windows: doble clic -->
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.VideoTapCommand}" CommandParameter="{Binding .}">
                                    <TapGestureRecognizer.NumberOfTapsRequired>
                                        <OnPlatform x:TypeArguments="x:Int32">
                                            <On Platform="iOS" Value="1" />
                                            <On Platform="MacCatalyst" Value="2" />
                                            <On Platform="WinUI" Value="2" />
                                        </OnPlatform>
                                    </TapGestureRecognizer.NumberOfTapsRequired>
                                </TapGestureRecognizer>
                                <!-- MacCatalyst/Windows: clic simple selecciona (deshabilitado en iOS con 99 taps) -->
                                <TapGestureRecognizer Tapped="OnVideoItemSingleTappedInternal">
                                    <TapGestureRecognizer.NumberOfTapsRequired>
                                        <OnPlatform x:TypeArguments="x:Int32">
                                            <On Platform="iOS" Value="99" />
                                            <On Platform="MacCatalyst" Value="1" />
                                            <On Platform="WinUI" Value="1" />
                                        </OnPlatform>
                                    </TapGestureRecognizer.NumberOfTapsRequired>
                                </TapGestureRecognizer>
                                <TapGestureRecognizer Buttons="Secondary" Tapped="OnVideoItemContextMenuTappedInternal">
                                    <TapGestureRecognizer.NumberOfTapsRequired>
                                        <OnPlatform x:TypeArguments="x:Int32">
                                            <On Platform="iOS" Value="99" />
                                            <On Platform="MacCatalyst,WinUI" Value="1" />
                                        </OnPlatform>
                                    </TapGestureRecognizer.NumberOfTapsRequired>
                                </TapGestureRecognizer>
                                <DragGestureRecognizer CanDrag="True" DragStarting="OnDragStartingInternal" />
                            </Border.GestureRecognizers>
                            <Border.Triggers>
                                <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="Stroke" Value="#FF0066" />
                                    <Setter Property="StrokeThickness" Value="2" />
                                </DataTrigger>
                            </Border.Triggers>

                            <controls:AspectRatioContainer AspectRatio="1.777" MinimumHeightRequest="100">
                                <Grid>
                                    <!--  Behavior de long press para preview en iOS  -->
                                    <Grid.Behaviors>
                                        <!-- LongPressVideoPreviewBehavior solo activo en iOS (código protegido con #if IOS) -->
                                        <behaviors:LongPressVideoPreviewBehavior />
                                    </Grid.Behaviors>
                                    <Image
                                        Aspect="AspectFill"
                                        BackgroundColor="{StaticResource Gray300}"
                                        Source="{Binding EffectiveThumbnailPath}" />

                                    <!--  Overlay de play (oculto cuando está seleccionado en multiselect)  -->
                                    <controls:SymbolIcon
                                        HeightRequest="{StaticResource IconSizeXXLarge}"
                                        SymbolName="play.circle.fill"
                                        TintColor="White"
                                        Opacity="0.5"
                                        VerticalOptions="Center"
                                        WidthRequest="{StaticResource IconSizeXXLarge}">
                                        <controls:SymbolIcon.IsVisible>
                                            <OnPlatform x:TypeArguments="x:Boolean">
                                                <On Platform="MacCatalyst" Value="False" />
                                                <On Platform="Default" Value="True" />
                                            </OnPlatform>
                                        </controls:SymbolIcon.IsVisible>
                                    </controls:SymbolIcon>

                                    <!--  Checkmark de selección  -->
                                    <Border
                                        BackgroundColor="#34000000"
                                        HeightRequest="32"
                                        HorizontalOptions="End"
                                        Margin="6"
                                        StrokeShape="RoundRectangle 16"
                                        VerticalOptions="Start"
                                        WidthRequest="32">
                                        <Border.IsVisible>
                                            <MultiBinding Converter="{StaticResource AllTrueConverter}">
                                                <Binding Path="IsSelected" />
                                                <Binding>
                                                    <Binding.Source>
                                                        <OnPlatform x:TypeArguments="x:Boolean">
                                                            <On Platform="MacCatalyst" Value="False" />
                                                            <On Platform="Default" Value="True" />
                                                        </OnPlatform>
                                                    </Binding.Source>
                                                </Binding>
                                            </MultiBinding>
                                        </Border.IsVisible>
                                        <controls:SymbolIcon
                                            HeightRequest="{StaticResource IconSizeLarge}"
                                            HorizontalOptions="Center"
                                            SymbolName="checkmark"
                                            TintColor="LightGray"
                                            VerticalOptions="Center"
                                            WidthRequest="{StaticResource IconSizeLarge}" />
                                    </Border>

                                    <!--  Botón de menú (tres puntos)  -->
                                    <Border
                                        Margin="6"
                                        HeightRequest="28"
                                        WidthRequest="28"
                                        HorizontalOptions="End"
                                        VerticalOptions="End"
                                        BackgroundColor="#66000000"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 14">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnVideoItemContextMenuTappedInternal" />
                                        </Border.GestureRecognizers>
                                        <controls:SymbolIcon
                                            HeightRequest="14"
                                            WidthRequest="14"
                                            SymbolName="ellipsis.circle"
                                            TintColor="White"
                                            HorizontalOptions="Center"
                                            VerticalOptions="Center" />
                                    </Border>

                                    <!--  Indicador de video comparativo  -->
                                    <Border
                                        BackgroundColor="#CC000000"
                                        HeightRequest="28"
                                        HorizontalOptions="Start"
                                        Margin="6"
                                        Padding="6,4"
                                        StrokeShape="RoundRectangle 4"
                                        StrokeThickness="0"
                                        VerticalOptions="Start">
                                        <Border.IsVisible>
                                            <MultiBinding Converter="{StaticResource AllTrueConverter}">
                                                <Binding Path="IsComparisonVideo" />
                                                <Binding>
                                                    <Binding.Source>
                                                        <OnPlatform x:TypeArguments="x:Boolean">
                                                            <On Platform="MacCatalyst" Value="False" />
                                                            <On Platform="Default" Value="True" />
                                                        </OnPlatform>
                                                    </Binding.Source>
                                                </Binding>
                                            </MultiBinding>
                                        </Border.IsVisible>
                                        <HorizontalStackLayout Spacing="4">
                                            <controls:SymbolIcon
                                                HeightRequest="{StaticResource IconSizeMedium}"
                                                SymbolName="rectangle.on.rectangle"
                                                TintColor="#FF0066"
                                                VerticalOptions="Center"
                                                WidthRequest="{StaticResource IconSizeMedium}" />
                                            <Label
                                                FontSize="{StaticResource FontSizeCaption}"
                                                Text="VS"
                                                TextColor="#FF0066"
                                                VerticalOptions="Center"
                                                FontAttributes="None" />
                                        </HorizontalStackLayout>
                                    </Border>

                                    <!--  Indicador de tiempo/split  -->
                                    <Border
                                        BackgroundColor="#CC000000"
                                        HeightRequest="28"
                                        HorizontalOptions="Start"
                                        Margin="6"
                                        Padding="6,4"
                                        StrokeShape="RoundRectangle 4"
                                        StrokeThickness="0"
                                        VerticalOptions="End">
                                        <Border.IsVisible>
                                            <MultiBinding Converter="{StaticResource AllTrueConverter}">
                                                <Binding Path="HasTiming" />
                                                <Binding>
                                                    <Binding.Source>
                                                        <OnPlatform x:TypeArguments="x:Boolean">
                                                            <On Platform="MacCatalyst" Value="False" />
                                                            <On Platform="Default" Value="True" />
                                                        </OnPlatform>
                                                    </Binding.Source>
                                                </Binding>
                                            </MultiBinding>
                                        </Border.IsVisible>
                                        <controls:SymbolIcon
                                            HeightRequest="{StaticResource IconSizeMedium}"
                                            SymbolName="timer"
                                            TintColor="White"
                                            VerticalOptions="Center"
                                            WidthRequest="{StaticResource IconSizeMedium}" />
                                    </Border>

                                    <!--  Indicador de favorito  -->
                                    <Border
                                        BackgroundColor="#CC000000"
                                        HeightRequest="26"
                                        HorizontalOptions="Start"
                                        Margin="6,34,0,0"
                                        Padding="6,2"
                                        StrokeShape="RoundRectangle 4"
                                        StrokeThickness="0"
                                        VerticalOptions="Start"
                                        IsVisible="{Binding IsFavorite, Converter={StaticResource IntToBoolConverter}}">
                                        <controls:SymbolIcon
                                            HeightRequest="{StaticResource IconSizeMedium}"
                                            WidthRequest="{StaticResource IconSizeMedium}"
                                            SymbolName="star.fill"
                                            TintColor="#FFFFAB40"
                                            VerticalOptions="Center" />
                                    </Border>

                                    <!--  Indicador de origen: organización descargado / solo referenciado  -->
                                    <Border
                                        BackgroundColor="{Binding SyncStatusColor}"
                                        HeightRequest="24"
                                        HorizontalOptions="End"
                                        Margin="6"
                                        Padding="6,2"
                                        StrokeShape="RoundRectangle 12"
                                        StrokeThickness="0"
                                        VerticalOptions="Start"
                                        IsVisible="{Binding ShowSyncBadge}">
                                        <controls:SymbolIcon
                                            HeightRequest="12"
                                            WidthRequest="12"
                                            SymbolName="{Binding SyncStatusIcon}"
                                            TintColor="White"
                                            VerticalOptions="Center"
                                            HorizontalOptions="Center" />
                                    </Border>
                                </Grid>
                            </controls:AspectRatioContainer>
                        </Border>

                        <!--  Fila 1: Nombre del atleta  -->
                        <Label
                            Grid.Row="1"
                            FontSize="{StaticResource FontSizeSubtitle}"
                            FontAttributes="Bold"
                            HorizontalTextAlignment="Center"
                            LineBreakMode="TailTruncation"
                            Text="{Binding DisplayLine1}"
                            TextColor="White" />

                        <!--  Fila 2: Lugar, fecha y hora  -->
                        <Label
                            Grid.Row="2"
                            FontSize="{StaticResource FontSizeBody}"
                            HorizontalTextAlignment="Center"
                            LineBreakMode="TailTruncation"
                            Text="{Binding DisplayLine2}"
                            TextColor="White" />

                        <!--  Fila 3: Etiquetas  -->
                        <HorizontalStackLayout
                            Grid.Row="3"
                            HorizontalOptions="Center"
                            Spacing="4"
                            IsVisible="{Binding HasTagsSummary}">
                            <controls:SymbolIcon
                                HeightRequest="{StaticResource IconSizeSmall}"
                                SymbolName="tag.fill"
                                TintColor="White"
                                VerticalOptions="Center"
                                WidthRequest="{StaticResource IconSizeSmall}" />
                            <Label
                                FontSize="{StaticResource FontSizeSmall}"
                                LineBreakMode="TailTruncation"
                                Text="{Binding TagsSummary}"
                                TextColor="White"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <VerticalStackLayout
                    Padding="40"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                    <Label
                        FontSize="{StaticResource FontSizeTitle}"
                        HorizontalOptions="Center"
                        Text="Selecciona una sesión"
                        TextColor="Gray" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.Footer>
                <VerticalStackLayout
                    Padding="20"
                    HorizontalOptions="Center"
                    IsVisible="{Binding Videos.HasMoreVideos}">
                    <ActivityIndicator
                        HeightRequest="24"
                        IsRunning="{Binding Videos.IsLoadingMore}"
                        IsVisible="{Binding Videos.IsLoadingMore}"
                        WidthRequest="24"
                        Color="{StaticResource Primary}" />
                    <Label
                        FontSize="{StaticResource FontSizeSmall}"
                        HorizontalOptions="Center"
                        IsVisible="{Binding Videos.HasMoreVideos}"
                        Text="Desplázate para cargar más..."
                        TextColor="Gray" />
                </VerticalStackLayout>
            </CollectionView.Footer>
        </CollectionView>

        <!--  Grid de sesiones remotas (tarjetas cuadradas estilo Apple Music) — Biblioteca de Organización  -->
        <CollectionView
            x:Name="RemoteSessionsGrid"
            Grid.Row="2"
            ItemsSource="{Binding Remote.RemoteSessions}"
            IsVisible="{Binding Remote.ShowRemoteGallery}"
            SelectionMode="None"
            Margin="{StaticResource MarginTopSmall}"
            ItemSizingStrategy="MeasureFirstItem">
            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    HorizontalItemSpacing="20"
                    Orientation="Vertical"
                    Span="3"
                    VerticalItemSpacing="20" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:RemoteSessionListItem">
                    <Border
                        BackgroundColor="{Binding CardBackgroundColor}"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 16"
                        StrokeThickness="3"
                        Padding="0">
                        <Border.Triggers>
                            <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="Stroke" Value="#FF0066" />
                            </DataTrigger>
                        </Border.Triggers>
                        <Border.Behaviors>
                            <behaviors:HoverBackgroundBehavior />
                        </Border.Behaviors>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.Remote.SelectRemoteSessionCommand}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>

                        <controls:AspectRatioContainer AspectRatio="1.05">
                            <Grid>
                                <!--  Capa 0: Imagen de fondo (thumbnail del primer video)  -->
                                <Image
                                    Aspect="AspectFill"
                                    Source="{Binding ThumbnailSource}"
                                    IsVisible="{Binding HasThumbnail}" />

                                <!--  Capa 1: Overlay uniforme  -->
                                <BoxView
                                    BackgroundColor="{Binding CardBackgroundColor}"
                                    Opacity="0.92"
                                    IsVisible="{Binding HasThumbnail}" />



                                <!--  Capa 3: Color sólido de fondo (visible cuando no hay thumbnail)  -->
                                <BoxView BackgroundColor="{Binding CardBackgroundColor}">
                                    <BoxView.Triggers>
                                        <DataTrigger TargetType="BoxView" Binding="{Binding HasThumbnail}" Value="True">
                                            <Setter Property="IsVisible" Value="False" />
                                        </DataTrigger>
                                    </BoxView.Triggers>
                                </BoxView>

                                <!--  Capa 4: Contenido de la tarjeta  -->
                                <Grid Padding="10" RowDefinitions="Auto,*,Auto">

                                <!--  Row 0: Badge de fecha + nº vídeos (esquina superior)  -->
                                <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" VerticalOptions="Start">
                                    <!--  Bloque fecha  -->
                                    <Border
                                        Grid.Column="0"
                                        Padding="8,5"
                                        BackgroundColor="{Binding CardAccentColor}"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 10">
                                        <VerticalStackLayout Spacing="0" HorizontalOptions="Center">
                                            <Label
                                                FontSize="20"
                                                FontAttributes="Bold"
                                                Text="{Binding DayText}"
                                                TextColor="White"
                                                HorizontalTextAlignment="Center"
                                                LineBreakMode="NoWrap" />
                                            <Label
                                                FontSize="9"
                                                FontAttributes="Bold"
                                                Text="{Binding MonthText}"
                                                TextColor="#CCFFFFFF"
                                                HorizontalTextAlignment="Center"
                                                Margin="0,-2,0,0" />
                                        </VerticalStackLayout>
                                    </Border>

                                    <!--  Badge de nº de vídeos (esquina superior derecha)  -->
                                    <Border
                                        Grid.Column="2"
                                        Padding="6,3"
                                        BackgroundColor="#40FFFFFF"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 8"
                                        VerticalOptions="Start">
                                        <HorizontalStackLayout Spacing="3">
                                            <controls:SymbolIcon
                                                HeightRequest="10"
                                                WidthRequest="10"
                                                SymbolName="video.fill"
                                                TintColor="White"
                                                VerticalOptions="Center" />
                                            <Label
                                                FontSize="10"
                                                FontAttributes="Bold"
                                                Text="{Binding VideoCountText}"
                                                TextColor="White"
                                                VerticalOptions="Center"
                                                LineBreakMode="NoWrap" />
                                        </HorizontalStackLayout>
                                    </Border>
                                </Grid>

                                <!--  Row 1: Lugar + Nombre (centrado, con margen inferior para no solapar Row 2)  -->
                                <VerticalStackLayout Grid.Row="1" VerticalOptions="Center" HorizontalOptions="Fill" Spacing="4" Margin="0,0,0,100">
                                    <Label
                                        FontAttributes="Bold"
                                        Text="{Binding PlaceDisplay}"
                                        TextColor="{Binding CardPlaceTextColor}"
                                        HorizontalTextAlignment="Center"
                                        LineBreakMode="TailTruncation"
                                        MaxLines="2"
                                        IsVisible="{Binding HasPlace}">
                                        <Label.Behaviors>
                                            <behaviors:AutoFitFontBehavior MaxFontSize="40" MinFontSize="12" />
                                        </Label.Behaviors>
                                    </Label>
                                    <Label
                                        FontSize="16"
                                        Text="{Binding Title}"
                                        TextColor="#EEFFFFFF"
                                        HorizontalTextAlignment="Center"
                                        LineBreakMode="TailTruncation"
                                        MaxLines="2" />
                                </VerticalStackLayout>

                                <!--  Row 2: Coach + Hora (inferior)  -->
                                <HorizontalStackLayout Grid.Row="2" Spacing="8" HorizontalOptions="Center" Margin="0,0,0,2">
                                    <controls:SymbolIcon
                                        HeightRequest="11"
                                        WidthRequest="11"
                                        SymbolName="person.fill"
                                        TintColor="#AAFFFFFF"
                                        VerticalOptions="Center" />
                                    <Label
                                        FontSize="11"
                                        Text="{Binding CoachText}"
                                        TextColor="#CCFFFFFF"
                                        VerticalOptions="Center"
                                        LineBreakMode="TailTruncation" />
                                    <controls:SymbolIcon
                                        HeightRequest="11"
                                        WidthRequest="11"
                                        SymbolName="clock"
                                        TintColor="#88FFFFFF"
                                        VerticalOptions="Center" />
                                    <Label
                                        FontSize="11"
                                        Text="{Binding TimeText}"
                                        TextColor="#AAFFFFFF"
                                        VerticalOptions="Center" />
                                </HorizontalStackLayout>

                            </Grid>
                            </Grid>
                        </controls:AspectRatioContainer>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <VerticalStackLayout
                    Padding="40"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                    <controls:SymbolIcon
                        HeightRequest="64"
                        WidthRequest="64"
                        SymbolName="building.2"
                        TintColor="Gray"
                        HorizontalOptions="Center"
                        Margin="0,0,0,16" />
                    <Label
                        FontSize="{StaticResource FontSizeTitle}"
                        HorizontalOptions="Center"
                        Text="No hay sesiones en la organización"
                        TextColor="Gray" />
                    <Label
                        FontSize="{StaticResource FontSizeBody}"
                        HorizontalOptions="Center"
                        Text="Importa sesiones para verlas aquí"
                        TextColor="#666666"
                        Margin="0,8,0,0" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.Header>
                <ActivityIndicator
                    HeightRequest="32"
                    WidthRequest="32"
                    IsRunning="{Binding Remote.IsLoadingRemoteVideos}"
                    IsVisible="{Binding Remote.IsLoadingRemoteVideos}"
                    Color="#FF0066"
                    HorizontalOptions="Center"
                    Margin="0,20,0,0" />
            </CollectionView.Header>
        </CollectionView>

        <CollectionView
            x:Name="VideoLessonsGallery"
            Grid.Row="2"
            ItemsSource="{Binding Videos.VideoLessons}"
            IsVisible="{Binding Layout.IsVideoLessonsSelected}"
            SelectionMode="None"
            Margin="{StaticResource MarginTopSmall}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    HorizontalItemSpacing="{StaticResource SpacingXSmall}"
                    Orientation="Vertical"
                    Span="{Binding Videos.VideoGalleryColumnSpan}"
                    VerticalItemSpacing="{StaticResource SpacingSmall}" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:VideoLesson">
                    <Border
                        BackgroundColor="Transparent"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 4"
                        StrokeThickness="0">
                        <Border.Behaviors>
                            <behaviors:HoverBackgroundBehavior />
                        </Border.Behaviors>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.VideoLessonTapCommand}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>

                        <Grid RowDefinitions="Auto,Auto">
                            <!--  Thumbnail con aspect ratio 16:9  -->
                            <controls:AspectRatioContainer AspectRatio="1.777">
                                <Grid>
                                    <Image
                                        Aspect="AspectFill"
                                        BackgroundColor="{StaticResource Gray300}"
                                        Source="{Binding EffectiveThumbnailPath}" />

                                    <!--  Overlay de play  -->
                                    <controls:SymbolIcon
                                        HeightRequest="{StaticResource IconSizeXXLarge}"
                                        SymbolName="play.circle.fill"
                                        TintColor="Black"
                                        Opacity="0.5"
                                        VerticalOptions="Center"
                                        WidthRequest="{StaticResource IconSizeXXLarge}" />
                                </Grid>
                            </controls:AspectRatioContainer>

                            <!--  Footer con info y acciones  -->
                            <Grid
                                Grid.Row="1"
                                Padding="8"
                                ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Grid.Column="0" Spacing="2">
                                    <Label
                                        FontSize="{StaticResource FontSizeSmall}"
                                        LineBreakMode="TailTruncation"
                                        Text="{Binding DisplayTitle}"
                                        TextColor="White" />
                                    <Label
                                        FontSize="{StaticResource FontSizeSmall}"
                                        LineBreakMode="TailTruncation"
                                        Text="{Binding SessionDisplayName}"
                                        TextColor="#FFCCCCCC" />
                                    <Label
                                        FontSize="{StaticResource FontSizeSmall}"
                                        Text="{Binding CreatedAtUtc, StringFormat='{0:dd/MM/yyyy HH:mm}'}"
                                        TextColor="#FFCCCCCC" />
                                </VerticalStackLayout>

                                <!--  Botones de acción  -->
                                <HorizontalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                    <Border
                                        WidthRequest="{StaticResource ElementHeightMedium}"
                                        HeightRequest="{StaticResource ElementHeightMedium}"
                                        Padding="{StaticResource PaddingXSmall}"
                                        BackgroundColor="#FF3A3A3A"
                                        StrokeShape="RoundRectangle 14"
                                        StrokeThickness="0">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.ShareVideoLessonCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                        <controls:SymbolIcon
                                            WidthRequest="{StaticResource IconSizeSmall}"
                                            HeightRequest="{StaticResource IconSizeSmall}"
                                            SymbolName="square.and.arrow.up"
                                            TintColor="White"
                                            HorizontalOptions="Center"
                                            VerticalOptions="Center" />
                                    </Border>
                                    <Border
                                        WidthRequest="{StaticResource ElementHeightMedium}"
                                        HeightRequest="{StaticResource ElementHeightMedium}"
                                        Padding="{StaticResource PaddingXSmall}"
                                        BackgroundColor="#FF3A3A3A"
                                        StrokeShape="RoundRectangle 14"
                                        StrokeThickness="0">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.DeleteVideoLessonCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                        <controls:SymbolIcon
                                            WidthRequest="{StaticResource IconSizeSmall}"
                                            HeightRequest="{StaticResource IconSizeSmall}"
                                            SymbolName="trash"
                                            TintColor="#FFFF6B6B"
                                            HorizontalOptions="Center"
                                            VerticalOptions="Center" />
                                    </Border>
                                </HorizontalStackLayout>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <VerticalStackLayout
                    Padding="40"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                    <Label
                        FontSize="{StaticResource FontSizeTitle}"
                        HorizontalOptions="Center"
                        Text="No hay videolecciones"
                        TextColor="Gray" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <!--  Videolecciones de Organización  -->
        <CollectionView
            x:Name="RemoteVideoLessonsGallery"
            Grid.Row="2"
            ItemsSource="{Binding Remote.RemoteVideoLessons}"
            IsVisible="{Binding Remote.ShowRemoteVideoLessons}"
            SelectionMode="None"
            Margin="{StaticResource MarginTopSmall}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    HorizontalItemSpacing="{StaticResource SpacingXSmall}"
                    Orientation="Vertical"
                    Span="{Binding Videos.VideoGalleryColumnSpan}"
                    VerticalItemSpacing="{StaticResource SpacingSmall}" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:VideoLesson">
                    <Border
                        BackgroundColor="Transparent"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 4"
                        StrokeThickness="0">
                        <Border.Behaviors>
                            <behaviors:HoverBackgroundBehavior />
                        </Border.Behaviors>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.Remote.RemoteVideoLessonTapCommand}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>

                        <Grid RowDefinitions="Auto,Auto">
                            <!--  Thumbnail con aspect ratio 16:9  -->
                            <controls:AspectRatioContainer AspectRatio="1.777">
                                <Grid>
                                    <Image
                                        Aspect="AspectFill"
                                        BackgroundColor="{StaticResource Gray300}"
                                        Source="{Binding EffectiveThumbnailPath}" />

                                    <!--  Overlay de play  -->
                                    <controls:SymbolIcon
                                        HeightRequest="{StaticResource IconSizeXXLarge}"
                                        SymbolName="play.circle.fill"
                                        TintColor="Black"
                                        Opacity="0.5"
                                        VerticalOptions="Center"
                                        WidthRequest="{StaticResource IconSizeXXLarge}" />
                                </Grid>
                            </controls:AspectRatioContainer>

                            <!--  Footer con info y acciones  -->
                            <Grid
                                Grid.Row="1"
                                Padding="8"
                                ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Grid.Column="0" Spacing="2">
                                    <Label
                                        FontSize="{StaticResource FontSizeSmall}"
                                        LineBreakMode="TailTruncation"
                                        Text="{Binding DisplayTitle}"
                                        TextColor="White" />
                                    <Label
                                        FontSize="{StaticResource FontSizeSmall}"
                                        LineBreakMode="TailTruncation"
                                        Text="{Binding SessionDisplayName}"
                                        TextColor="#FFCCCCCC" />
                                    <Label
                                        FontSize="{StaticResource FontSizeSmall}"
                                        Text="{Binding CreatedAtUtc, StringFormat='{0:dd/MM/yyyy HH:mm}'}"
                                        TextColor="#FFCCCCCC" />
                                </VerticalStackLayout>

                                <!--  Botones de acción  -->
                                <HorizontalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                    <Border
                                        WidthRequest="{StaticResource ElementHeightMedium}"
                                        HeightRequest="{StaticResource ElementHeightMedium}"
                                        Padding="{StaticResource PaddingXSmall}"
                                        BackgroundColor="#FF3A3A3A"
                                        StrokeShape="RoundRectangle 14"
                                        StrokeThickness="0">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.Remote.ShareRemoteVideoLessonCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                        <controls:SymbolIcon
                                            WidthRequest="{StaticResource IconSizeSmall}"
                                            HeightRequest="{StaticResource IconSizeSmall}"
                                            SymbolName="square.and.arrow.up"
                                            TintColor="White"
                                            HorizontalOptions="Center"
                                            VerticalOptions="Center" />
                                    </Border>
                                    <Border
                                        WidthRequest="{StaticResource ElementHeightMedium}"
                                        HeightRequest="{StaticResource ElementHeightMedium}"
                                        Padding="{StaticResource PaddingXSmall}"
                                        BackgroundColor="#FF3A3A3A"
                                        StrokeShape="RoundRectangle 14"
                                        StrokeThickness="0">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type controls:DashboardVideosPanelView}}, Path=BindingContext.Remote.DeleteRemoteVideoLessonCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                        <controls:SymbolIcon
                                            WidthRequest="{StaticResource IconSizeSmall}"
                                            HeightRequest="{StaticResource IconSizeSmall}"
                                            SymbolName="trash"
                                            TintColor="#FFFF6B6B"
                                            HorizontalOptions="Center"
                                            VerticalOptions="Center" />
                                    </Border>
                                </HorizontalStackLayout>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <VerticalStackLayout
                    Padding="40"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                    <Label
                        FontSize="{StaticResource FontSizeTitle}"
                        HorizontalOptions="Center"
                        Text="No hay videolecciones de organización"
                        TextColor="Gray" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <!--  Vista Diario: Calendario  -->
        <controls:CalendarView
            Grid.Row="2"
            Margin="0,10,0,0"
            IsVisible="{Binding Layout.IsDiaryViewSelected}"
            SelectedDate="{Binding Diary.SelectedDiaryDate}"
            DateSelectedCommand="{Binding Diary.SelectDiaryDateCommand}"
            DiaryEntries="{Binding Diary.DiaryEntriesForMonth}"
            Sessions="{Binding Diary.SessionsForMonth}"
            WellnessData="{Binding Diary.WellnessDataForMonth}"
            AverageWellnessScore="{Binding Diary.AverageWellnessScore}"
            HorizontalOptions="Fill"
            VerticalOptions="Fill" />
    </Grid>
</ContentView>
